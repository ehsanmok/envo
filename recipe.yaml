package:
  name: envo
  version: "0.1.1"

source:
  path: .

build:
  script:
    - mkdir -p $PREFIX/lib/mojo
    - cp -r envo $PREFIX/lib/mojo/

requirements:
  host:
    - mojo ==0.26.3.0.dev2026042005
  run:
    - mojo ==0.26.3.0.dev2026042005

about:
  summary: Typed config loading from env vars, TOML, and CLI for Mojo
  license: MIT
